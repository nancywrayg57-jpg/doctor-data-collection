---
title: codex工程经验（医生画像采集项目）
date: 2026-08-11
project: 珠三角三甲医院医生画像采集
root: D:\workspace\信息收集整理
tags:
  - Codex
  - 医生画像
  - 官方数据采集
  - Obsidian
  - 多智能体协作
  - 数据质量
---

# codex工程经验（医生画像采集项目）

本文承接参考项目中的 Codex 工程经验，但已改写为本项目适用的执行规范。它不是需求文档，也不是路线图；它用于提醒 Codex 在执行采集、表格处理、画像生成、审计交接时避开已经暴露的问题。

配套文件：

1. 项目宪法：`D:\workspace\信息收集整理\Agent.md`
2. 执行路线图：`D:\workspace\信息收集整理\docs\2026-08-10_医生画像采集执行路线图.md`
3. 需求确认：`D:\workspace\信息收集整理\docs\2026-08-10_医生画像采集任务需求确认.md`
4. 架构决策目录：`D:\workspace\信息收集整理\docs\architecture_decisions`

## 1. 工作模式

### 1.1 先读后写

修改脚本、总底表、画像或文档前，必须先读 `Agent.md`、路线图、相关架构决策记录和目标文件。不要只凭上一段聊天记忆判断当前状态。

尤其要注意：路线图中的局部状态可能落后于后续架构决策和现场文件。执行前用实际文件、报告和脚本输出重新核验。

### 1.2 Claude 选院和审计试采，Codex 执行

本项目协作模式固定：

1. Claude 负责读取台账和总底表，选择已确认且未追加医院，同时下发医院官网首页与医生目录入口，并独立审计试采。
2. Codex 只执行明确指定的医院：试采、追加、检查、画像生成、清理、脚本升级和文档沉淀。
3. 管理员负责维护台账确认边界和裁决异常口径；当前已授权 Claude 试采审计 `通过` 或 `有条件通过` 后，Codex 可直接完成全量追加和画像，无需管理员二次确认或完成后二次业务审计。

Codex 不应把自己的脚本输出当成最终业务确认，也不得自行选择下一家医院。正式追加总底表前，固定提示词入口必须记录 Claude `通过` / `有条件通过` 结论和 `FULL_APPEND_AND_OBSIDIAN` 阶段。

### 1.3 GitHub 角色边界

本任务后续使用 GitHub 仓库版本化采集结果、脚本和文档：

```text
https://github.com/nancywrayg57-jpg/doctor-data-collection.git
```

角色固定：

1. Codex 是 developer，GitHub 账号为 `xtzhou247`，负责实现、采集、检查、分支提交和 PR。
2. Claude 是 owner，GitHub 账号为 `nancywrayg57-jpg`，负责审计 Codex 结果、指导下一步、输出 Codex 提示词、审批或合并 PR。
3. Codex 不直接推送或合并 `main`。
4. Claude 给 Codex 的下一步提示词应同步到 `D:\workspace\信息收集整理\docs\agent_prompts\codex_next_prompt.md`，Codex 每次启动时读取。

执行 GitHub 操作前先判断现场状态：

1. 如果当前目录不是 Git 仓库，只记录状态并等待管理员授权初始化、绑定远端或首次推送。
2. 如果已接入仓库，先检查分支、远端、登录账号、未提交文件，再创建 `codex/...` 分支工作。
3. Codex 推送、发 PR 或执行任何远端写入前，GitHub 登录身份必须是 `xtzhou247`；如果当前登录为 `nancywrayg57-jpg`，只能做只读检查或等待切换 developer 身份。
4. 提交前检查是否包含临时试采文件、无用 payload、隐私或第三方来源内容。
5. PR 描述中写清采集医院、试采/追加/画像结果、验证动作、异常提示和 Claude 审计材料。

### 1.4 诊断优先于修复

采集质量差时，不要直接写补丁。先确认是哪一类问题：

1. 入口台账链接错误。
2. 官网页面结构不适合通用模板。
3. 通用模板把科室、新闻、公告、招聘页误识别为医生页。
4. 字段解析错位。
5. 官网本身信息不足。
6. Excel 文件占用或写入中断。

连续 2 次试采或修复失败，停止继续改，输出根因假设和建议动作。

### 1.5 证据比口头结论重要

每轮交付至少留下：

1. 执行医院。
2. 医院官网首页链接。
3. 医生目录入口链接。
4. 适配器名称。
5. 试采文件路径。
6. 试采报告或更新报告路径。
7. 新增写入数、重复跳过数、异常提示数。
8. 总底表修改时间。
9. 是否清理临时文件。
10. 是否等待 Claude 试采审计，或是否已取得 `通过` / `有条件通过` 结论。

没有路径和数字的“已完成”不能作为完成证据。

## 2. 官方来源与合规经验

### 2.1 来源域名必须可解释

来源链接应来自医院官网或官方公开页面。若出现第三方平台、挂号平台、评价平台、百科或新闻聚合站，整批停止，不追加总底表。

### 2.2 官网缺失字段保持空白

医生简介短、职称不明确、科室不展示时，留空或写入 `异常提示`。不要用搜索、推断或营销语言补齐。

### 2.3 亮点不是包装文案

`亮眼经历线索` 只记录官方文本中的证据，例如疑难重症、复杂病例、难治疾病、重要医疗经历、科研或指南参与等。不要写成“权威专家”“治愈率高”“疗效显著”等对外宣传判断。

### 2.4 不采集患者侧内容

任何患者评价、满意度、排名、问诊内容、病例隐私都不进入本项目。即使公开可见，也不采集。

## 3. 入口台账经验

### 3.1 台账是医院入口单点来源

正式采集只从入口台账读取医院入口，不让管理员逐个复制详情页链接。

查看可采集 A 级医院：

```powershell
cd D:\workspace\信息收集整理
python .\work\collect_official_doctors_batch.py --list-targets
```

### 3.2 下一家医院由 Claude 或管理员下发

路线图中记录的“下一家医院”只是历史状态，Codex 不得据此自行选院。继续执行前必须确认固定提示词入口同时给出：

1. 目标医院在入口台账中仍为 `确认可采集`。
2. 目标医院未出现在总底表医院统计中。
3. 医院官网首页和医生目录入口均与台账一致，且是官方链接。
4. 台账序号不超过管理员当前标注边界 39。

### 3.3 按城市推进不等于无人批量追加

可以按城市批量准备候选，但正式追加仍应保留 Claude 试采审计门禁。Codex 完成单院追加和画像后必须停止，等待 Claude 选择下一家；通用模板不允许无复核连续写入多家医院。

## 4. 采集脚本经验

### 4.1 脚本定位

`collect_official_doctors_batch.py` 是半自动官方采集工具，负责读取台账、试采、标准化字段、去重、追加总底表和输出报告。它不是万能爬虫，也不是无人值守采集器。

### 4.2 常用命令

列出目标：

```powershell
cd D:\workspace\信息收集整理
python .\work\collect_official_doctors_batch.py --list-targets
```

试采 10 位，不写总底表：

```powershell
cd D:\workspace\信息收集整理
python .\work\collect_official_doctors_batch.py --hospital "目标医院名称" --trial-only --max-doctors 10 --no-xlsx
```

专用适配器正式追加：

```powershell
cd D:\workspace\信息收集整理
python .\work\collect_official_doctors_batch.py --hospital "目标医院名称"
```

通用模板正式追加：

```powershell
cd D:\workspace\信息收集整理
python .\work\collect_official_doctors_batch.py --hospital "目标医院名称" --allow-generic-append
```

### 4.3 通用模板的正确用法

通用模板适合先快速判断官网是否可自动解析。它不能替代人工样本复核。

通过标准：

1. 样本大部分是医生页面。
2. 姓名、科室、职称、擅长没有大面积错位。
3. 来源链接均为官方域名。
4. 没有批量混入新闻、公告、采购、科室介绍。

不通过时，不要求管理员手工清洗大量错误数据。应记录未通过医院，后续人工处理入口或新增专用适配器。

### 4.4 专用适配器不是默认选项

每家医院都写专用适配器效率很低。优先策略：

1. 入口台账确认官方入口。
2. 通用模板试采 10 位。
3. 质量合格则正式追加。
4. 质量不合格再考虑专用适配器。

## 5. 表格处理经验

### 5.1 Excel 占用是常见失败原因

正式追加前让管理员关闭总底表 Excel。若写入失败，先判断是否文件占用，不要立即怀疑采集逻辑。

### 5.2 XLSX 和 CSV 同时检查

总底表有 XLSX 和 CSV 镜像。正式追加后检查：

1. 两个文件修改时间是否更新。
2. 更新报告是否更新。
3. 总医生数是否增加。
4. 医院统计是否出现目标医院。
5. 异常提示是否异常升高。

### 5.3 总底表是正式数据资产

不要为每家医院保留单独正式底表，除非管理员明确要求。新增医生统一写入同一张总底表，靠 `医院` 字段区分来源。

### 5.4 报告字段口径可能滞后

历史报告中的 `待复核数` 沿用旧脚本字段口径，不等于要求人工复核全部医生。当前门禁口径是：试采样本经 Claude 审计 `通过` / `有条件通过` 后可正式追加；正式追加和画像完成后无需第二次业务审计，只重点处理 `异常提示` 不为空记录。

## 6. 长任务与进程经验

单家医院 700 位以上医生可能超过默认命令等待时间。此前中山大学附属第三医院正式追加曾超过 10 分钟，但后台进程继续完成。

经验：

1. 大医院追加前设置更长超时。
2. 不要在不确认后台进程状态的情况下重复启动正式追加。
3. 追加后以总底表、CSV、更新报告和进程状态综合判断是否完成。
4. 若疑似中断，先查目标医院行数和最近修改时间，再决定是否重跑。

## 7. Obsidian 画像经验

### 7.1 画像只从总底表生成

画像 Markdown 不直接抓官网，不额外补写第三方信息。画像内容来自总底表字段和管理员修正内容。

每次正式追加总底表并检查完成后，应立即执行 Obsidian 缺失画像补充生成，输出根目录固定为 `D:\workspace\信息收集整理\医生画像仓库\01_试点医院`；完成本院画像数量和索引核验后向 Claude 回报并停止。

### 7.2 不覆盖人工精修

生成脚本应默认保护已有人工精修画像。只有明确标记为自动生成的文件，或管理员授权 `--admin-allow-overwrite` 时，才允许覆盖。

### 7.3 计数核验不能省

进入画像生成、补充生成或验收前，必须核验：

1. 总底表医生数。
2. 各医院底表记录数。
3. 各医院 Markdown 文件数。
4. `_索引.md` 链接数。
5. 缺失画像数量。
6. 重名医生是否发生文件覆盖。

当前现场已发现画像报告和实际 Markdown 数量存在不一致。后续不要直接沿用“已生成 1993 份”的旧结论，必须重新跑数量核验或修复报告。

### 7.4 异常记录要进入清单

以下记录不应直接当成正式医生画像：

1. 姓名明显不是医生姓名。
2. 姓名字段异常过长。
3. 来源页明显是科室页、方向页或介绍页。
4. `异常提示` 指向字段错位或正文缺失。

## 8. 清理经验

### 8.1 只清理试采临时文件

可清理：

1. `work\*_trial_payload.json`
2. `work\*_trial_doctors.csv`
3. `work\*_trial_doctors.xlsx`
4. `work\*_trial_report.md`
5. `work\*_official_doctors_preview.png`

不可清理：

1. 入口台账。
2. 统一总底表。
3. 总底表 CSV。
4. 总底表更新报告。
5. 正式 payload，除非管理员明确要求。
6. 正式 Obsidian Markdown。

### 8.2 删除前必须列清单

清理前输出精确文件列表，说明保留哪些核心文件。不得使用不受控递归删除。

## 9. 文档沉淀经验

### 9.1 聊天不是事实源

重要结论写入：

```text
D:\workspace\信息收集整理\docs\architecture_decisions
```

每条记录应包含：

1. 背景。
2. 决策。
3. 当前事实。
4. 关键文件。
5. 风险。
6. 下一步。

### 9.2 状态冲突要显式记录

如果发现路线图、需求文档、报告、实际文件数量不一致，不要悄悄选择一个。应在当前交付和架构决策记录中标明：

1. 哪些文件冲突。
2. 当前现场核验结果是什么。
3. 后续执行应以什么为准。

### 9.3 交接必须结构化

跨 Agent 交接必须使用 `<Handoff_State>`。没有目标、路径、已完成、当前事实、下一步、约束和工件路径的交接不可执行。

## 10. 当前已知状态

截至 2026-08-10 本文生成时，已核验到：

1. 统一总底表更新报告显示：5 家医院、1993 位医生、37 条异常提示。
2. 已追加医院：中山大学附属第五医院、中山大学中山眼科中心、南部战区空军医院、中山大学肿瘤防治中心、中山大学附属第三医院。
3. 下一家医院由 Claude 或管理员现场读取台账和总底表后下发；Codex 不使用旧文档中的静态候选。管理员当前标注截止序号 39 `广州市中医院`。
4. Obsidian 画像已有生成和补充生成报告，但现场 Markdown 数量与部分报告结论不一致。下次处理画像前必须先做专项核验。

## 11. 快速检查清单

采集前：

- [ ] 是否读了 `Agent.md`、路线图、需求文档、最新架构决策？
- [ ] 是否确认 Claude 或管理员已下发目标医院、官网首页和医生目录入口？
- [ ] 是否确认目标医院在入口台账中为 `确认可采集`？
- [ ] 是否确认目标医院未在总底表中正式追加？
- [ ] 是否确认管理员关闭了总底表 Excel？
- [ ] 是否确认官网首页和医生目录入口都是医院官方公开页面？

试采后：

- [ ] 是否输出试采 CSV、报告、官网首页、医生目录入口、适配器、异常提示？
- [ ] 是否确认样本主要为真实医生？
- [ ] 是否检查字段是否大面积错位？
- [ ] 是否停止并等待 Claude 试采审计？
- [ ] 如果 Claude 审计已通过，是否已按完整执行指令直接追加而不是等待二次确认？

追加后：

- [ ] 是否检查新增数、重复数、异常提示数？
- [ ] 是否检查 XLSX、CSV、更新报告修改时间？
- [ ] 是否确认没有重复启动写表进程？
- [ ] 是否清理试采临时文件并保留核心文件？
- [ ] 是否更新架构决策或交接状态？

画像生成前：

- [ ] 是否确认总底表记录数？
- [ ] 是否确认 Markdown 文件数和索引链接数？
- [ ] 是否确认不覆盖人工精修画像？
- [ ] 是否跳过或标记异常姓名、非医生页、来源缺失记录？
- [ ] 是否生成或更新画像报告？
- [ ] 是否确认本院画像完成后已向 Claude 回报并停止，没有自行进入下一家医院？
