# Issue #47 广东药科大学附属第一医院照片 TRIAL

> 日期：2026-08-15
> GitHub Issue：<https://github.com/nancywrayg57-jpg/doctor-data-collection/issues/47>
> 分支：`codex/mhrj/issue-47-gy120-photo-trial`
> Phase：`TRIAL_READY_FOR_OWNER_AUDIT`
> 医院：广东药科大学附属第一医院
> 官网：<https://www.gy120.net/>
> 医生目录：<https://www.gy120.net/zhuanjia.asp>

## 1. 目标与边界

本轮只完成官网目录普查、349 个唯一 ArticleID 顺序详情核验、纯护理身份排除、10 位医生试采和本人职业照审计材料，不写入统一总底表，不生成正式 Obsidian 画像，不进入 FULL。

固定边界：

1. 只使用 `gy120.net` 官网公开目录、数字 ArticleID 详情页和官方图片路径。
2. 详情只接受 `https://www.gy120.net/ArticleShow.asp?ArticleID=<数字>`，query 不得包含其他参数。
3. 旧 ASP 页面响应头没有 charset、meta 错标 UTF-8；响应体按 GB18030 严格解码，替换字符或高置信乱码标记触发写出前门禁。
4. 推荐区只作为角色证据，必须与科室树 ArticleID 重叠；正式身份按 ArticleID 去重，不按姓名猜测归并。
5. 只接受同域 `/files/<文件名>` 本人职业照；占位、空图、越界或其他旧路径只计数，不进入试采照片。
6. 图片请求只携带对应同域公开详情页 `Referer`。首次 HTTP 非 200 或 `Timeout` / `ConnectionError` / `ChunkedEncodingError` / `IncompleteRead` 后等待 1 秒，以同一 URL、同一 Referer、同一请求头只重试一次；不手工注入 Cookie、不绕过验证。连续两次失败按管理员裁决留空并标注。
7. 详情按顺序请求，不并发访问旧 IIS；FULL 在联网前硬熔断。

## 2. 官网普查结果

### 2.1 推荐区与科室树

| 类别 | 科室数 | 医生—科室关系 |
|---|---:|---:|
| 内科 | 9 | 56 |
| 外科 | 12 | 79 |
| 其他科室 | 28 | 157 |
| 医技 | 8 | 57 |
| 合计 | 57 | 349 |

- 空科室：3，分别为感染科共和诊室、门诊(普通外科)、理疗科。
- 唯一数字 ArticleID：349；跨科室重复 ID：0。
- 推荐区：58 次出现、56 个唯一 ID；首席专家 14 次、科室负责人 44 次；58 次均与科室树重叠。
- 交接前的分类估值为“其他科室 29 / 医技 7”，现场严格 DOM 复核结果为“其他科室 28 / 医技 8”，已按官网当前结构修正并由门禁固定。

### 2.2 详情、身份与出诊点

- 349/349 个详情顺序公开 GET 成功；详情错误 0，列表/详情姓名不一致 0。
- 纯护理身份排除 2：何淑敏（ArticleID 404，护师）、李巧姬（ArticleID 603，副主任护师/护士长）。
- 排除后医生候选：347。
- 官网公开出诊点标签计数：健康管理中心 159、农林门诊 80、共和门诊 34；26 个详情含多个出诊点。
- 只把官网明确的地点标签附在试采科室字段；星期、日期、上午/下午等排班时段写入正式字段数为 0。
- GB18030 解码替换字符 0、高置信乱码标记 0、私用区字符 0。

### 2.3 照片普查

| 状态 | 数量 |
|---|---:|
| 严格 `/files/<文件名>` 本人职业照可得 | 346 |
| 占位图 | 0 |
| 空图 | 0 |
| 严格路径拒绝 | 1 |

唯一拒绝项是丁彩萍（ArticleID 42）的官网旧路径 `upsfile/丁彩萍.jpg`。该路径不符合 Issue 预核验与本适配器固定的 `/files/<文件名>` 范围，本轮不扩大白名单、不下载、不补造；详情本身仍保留在 349-ID 对账中，等待 owner 审计是否维持该边界。

## 3. 实现决策

新增专用适配器 `gy120_asp_department_expert_photo`：

1. 精确识别唯一授权目录 URL，严格 GB18030 解码并校验医院/页面身份。
2. 只解析 `#a1 .index_list` 科室树；推荐区单独留痕，按数字 ArticleID 与科室树交叉对账。
3. 349 个详情只做顺序公开 GET；网络读取沿用有限三次详情请求，不使用并发。
4. 纯护理身份只依据详情“专业职称”排除，不按科室名称推断。
5. 试采从列表/详情姓名一致、身份非护理、严格本人职业照可得的候选中选 10 人；先保证首席专家、科室负责人、非推荐医生各至少 1 人，再最大化科室覆盖。
6. `.part2` 只提取官网出诊点标签；排班时段不进入正式字段。.part3 继续排除排名/患者片段、患者案例和导航噪声。
7. 图片保持官网原字节不压缩，记录 URL、详情 Referer、字节数、SHA-256、魔数、宽高、重试次数和两次失败结果。
8. 连续两次图片失败时清空照片字段并标注，同时清空重点标签、降为普通优先级；本次真实 TRIAL 未触发该降级。
9. FULL 模式在创建网络会话前硬熔断，必须等待 owner 在关联 PR 明确审计通过并切换 `FULL_APPEND_AND_OBSIDIAN`。
10. 新增 `work/tests/test_gy120_photo_trial.py`，覆盖严格入口/详情/照片 URL、GB18030、目录/推荐区、详情清洗、选样、同请求有界重试、连续响应体不完整留空、TRIAL 门禁和 FULL 熔断。

## 4. 最终 TRIAL 结果

### 4.1 样本与字段

- 试采：10 行、10 个唯一 ArticleID、10 张本人职业照。
- 科室覆盖：10，满足至少 3 个科室。
- 推荐角色覆盖：首席专家 4、科室负责人 5、非推荐医生 2；其中 1 人同时具备两种推荐角色。
- 详情失败 0；照片失败 0；图片有界重试实际触发 0。
- 正式字段排班时段残留 0、乱码 0、列表/详情姓名不一致 0、异常提示 0。
- 服务器下发的公开会话 Cookie 名称为 `ASPSESSIONIDAGQCTAAC`、`mycookie`；脚本未手工注入 Cookie，也未改变请求头绕过验证。

样本名单：周万兴、曾智桓、叶美莲、袁伟锋、吕路、叶健华、何兴祥、肖文豪、孙慧琳、区奕猛。

### 4.2 照片审计

10 张图片已逐张视觉复核，均为单人白大褂医务人员职业照；未发现患者、合影、新闻配图、案例影像或占位图。所有文件的原始字节、SHA-256、扩展名魔数和宽高均与 payload 对账一致。

| 指标 | 结果 |
|---|---:|
| 应采 / 实采 / 连续两次失败留空 | 10 / 10 / 0 |
| 总字节数 | 5,473,276 |
| 平均字节数 | 547,328 |
| 大图阈值命中 | 8 |
| 唯一重试实际触发 | 0 |

大图只留痕，沿用 owner 已批准的原图不压缩口径。

## 5. 总底表零变更证据

真实 TRIAL 前后以下三份受保护资产 SHA-256 完全一致：

| 文件 | 长度 | LastWriteTimeUtc | SHA-256 |
|---|---:|---|---|
| `珠三角三甲医院_医生画像自动采集总底表.xlsx` | 4,163,089 | `2026-08-15T02:44:38.4667515Z` | `C30AF03AD65842573437FD193D245C0E3D9FE260A4CA7BE029A5C0F02EA8C0EE` |
| `珠三角三甲医院_医生画像自动采集总底表.csv` | 16,496,052 | `2026-08-15T02:44:38.4301182Z` | `225C9EFF87685CB30535E05316D6E9B2C9DB8690E060DEAE9C25BEEEEEECEBCB` |
| `珠三角三甲医院_医生画像自动采集总底表_更新报告.md` | 5,089 | `2026-08-15T02:44:38.8006391Z` | `DE3FFA39FB8B7CDF533DBDAE6BF41B67B710BA60751D63F461BBBAA47A5EF3E0` |

## 6. 工件与验证闭环

| 工件 | 长度 | SHA-256 |
|---|---:|---|
| `work/广东药科大学附属第一医院_trial_payload.json` | 304,192 | `18A63679CA76E04AEC2F52C4796F16A403C27F18DC1EBC2E789A2ACA70EC79CC` |
| `work/广东药科大学附属第一医院_trial_doctors.csv` | 28,504 | `2AB5AC2AACBD03F2981AB3807BB7D0192BA571381601B9B41D5F9D3C581FEE32` |
| `work/广东药科大学附属第一医院_trial_report.md` | 94,561 | `E658771C2D06C3703C8C0A569C6436824F7A53F41C2AF8FB02D48A80951ECC42` |

以上为 Git 暂存/提交态 LF 字节长度与 SHA-256；Windows 工作树中的 CRLF 展开不作为 PR 工件哈希。

- `py_compile`：采集器与专项测试通过。
- GY120 专项测试：8 项通过。
- 全量 `unittest discover`：154 项通过。
- 真实 TRIAL：退出码 0，10 行、10 图、349 个详情错误 0、总底表零变更。
- 真实 payload 使用新增照片普查基线再次离线校验通过。
- 10 张照片视觉复核通过。
- `git diff --check`：通过。
- `git fsck --full --no-dangling`：通过。

## 7. 阻塞、根因、解决与防复发

### 7.1 旧 IIS 并发读取发生响应体不完整

- 阻塞：前期 4 并发只读详情普查在约 100 个请求后发生 `ChunkedEncodingError` / `IncompleteRead`。
- 根因：旧 IIS 对并发批量响应不稳定，不适合作为本院采集策略。
- 解决：GY120 全目录改为顺序请求，真实 TRIAL 349/349 详情成功。
- 防复发：适配器不提供并发入口；报告固定记录请求方式，真实执行只允许一个进程。

### 7.2 科室分类估值与现场 DOM 不一致

- 阻塞：交接估值为“其他科室 29 / 医技 7”。
- 根因：估值未按四个 `index_list_*` 容器重新逐项统计。
- 解决：现场入口解析得到“其他科室 28 / 医技 8”，总科室仍为 57、关系仍为 349。
- 防复发：写出前门禁同时固定四类科室数、四类关系数、57 科室和 3 空科室。

### 7.3 护理人数预估错误导致首次写出前停止

- 阻塞：首次真实验证被门禁拦截，实际护理排除 2、医生候选 347，与未验证常量 4/345 不符。
- 根因：常量 4 来自专门护理科室的预估，不是 349 个详情“专业职称”全量核验结果。
- 解决：只按详情职称识别到 ArticleID 404、603 两个纯护理身份，将基线修正为 2；第二次且最终真实验证成功。
- 防复发：报告保留两条逐 ID 排除表；门禁要求护理详情集合与排除集合完全相等。若后续再次出现现场漂移，停止并重新审计，禁止按科室名猜测。

### 7.4 旧 `upsfile` 图片路径不在授权范围

- 阻塞：ArticleID 42 的图片 `upsfile/丁彩萍.jpg` 不符合预期 `/files/<文件名>` 路径。
- 根因：官网存在一条旧版图片目录遗留。
- 解决：只标为 `rejected`，不下载、不补第三方图片，不影响详情对账。
- 防复发：照片 URL 函数只接受同域 `/files/<安全文件名>`；现场固定可得 346、拒绝 1，范围扩展必须由 owner 明确裁决。

## 8. 当前停止点

TRIAL 工件已完成本地验收。提交并推送原工作分支、创建关联 Issue #47 的 PR 后，停止等待 owner 对 TRIAL 明确审计。只有 owner 在该 PR 中给出 `通过` / `有条件通过` 且明确切换为 `FULL_APPEND_AND_OBSIDIAN`，才可进入 FULL；不得自行追加总底表、生成正式画像、合并 PR、关闭 Issue 或领取下一 Issue。

<Handoff_State>
Target: Issue #47 广东药科大学附属第一医院照片 TRIAL
AgentConstitution: D:\workspace\信息收集整理\Agent.md
RouteDoc: D:\workspace\信息收集整理\docs\2026-08-10_医生画像采集执行路线图.md
RequirementDoc: D:\workspace\信息收集整理\docs\2026-08-10_医生画像采集任务需求确认.md
GitHubIssue: https://github.com/nancywrayg57-jpg/doctor-data-collection/issues/47
Branch: codex/mhrj/issue-47-gy120-photo-trial
Phase: TRIAL_READY_FOR_OWNER_AUDIT
Completed:
- 57 个科室、3 个空科室、349 个唯一数字 ArticleID、推荐区 58 次/56 ID 完成全量对账
- 349 个详情顺序读取成功；2 个纯护理身份逐 ID 排除；347 个医生候选
- 10 行、10 科室、10 张本人职业照完成字节/SHA-256/魔数/宽高与视觉复核
- GB18030、出诊点标签、排班清洗、图片同请求唯一重试与 FULL 前置熔断已固定
- 总底表 XLSX/CSV/更新报告零变更
Next:
- 精确暂存代码、专项测试、10 图、三个 TRIAL 工件和本 ADR
- 复核身份后提交并以 SSH 非强制 fast-forward push 原分支
- 创建关联并 `Closes #47` 的 PR，等待 CI 后请求 owner TRIAL 审计
Constraints:
- 仅官网公开页面和医生本人职业照；禁患者/合影/新闻图/隐私、第三方和登录/验证码规避
- 图片只允许同 URL、同 Referer、同请求头的一次 1 秒后重试；连续两次失败留空并标注
- 旧 `upsfile/丁彩萍.jpg` 维持拒绝，除非 owner 明确扩展范围
- 不自行 FULL、合并 PR、关闭 Issue 或领取下一 Issue
Artifacts:
- D:\workspace\信息收集整理\work\广东药科大学附属第一医院_trial_payload.json
- D:\workspace\信息收集整理\work\广东药科大学附属第一医院_trial_doctors.csv
- D:\workspace\信息收集整理\work\广东药科大学附属第一医院_trial_report.md
- D:\workspace\信息收集整理\医生画像仓库\01_试点医院\广东药科大学附属第一医院\照片
- D:\workspace\信息收集整理\docs\architecture_decisions\2026-08-15_issue_47_gy120_photo_trial.md
</Handoff_State>
