# Agent.md - 珠三角三甲医院医生画像采集项目宪法

> 版本：v1.1
> 最后更新：2026-08-10
> 项目根目录：`D:\workspace\信息收集整理`
> 本文件定位：项目根目录下唯一 Agent 宪法文件。后续 Codex、Claude 或其他 Agent 接力时，先读本文件，再读路线图和架构决策记录。

---

## 0. 每次会话强制必读

新会话启动后，动手前必须读取以下文件，不得只凭聊天记录或记忆执行：

1. `D:\workspace\信息收集整理\Agent.md`
2. `D:\workspace\信息收集整理\docs\2026-08-10_医生画像采集执行路线图.md`
3. `D:\workspace\信息收集整理\docs\2026-08-10_医生画像采集任务需求确认.md`
4. `D:\workspace\信息收集整理\docs\architecture_decisions\` 下与当前任务相关的最新记录
5. 如存在 `D:\workspace\信息收集整理\docs\agent_prompts\codex_next_prompt.md`，Codex 必须在执行前读取该文件作为 Claude 或管理员留给 Codex 的最新可执行提示词

若路线图、需求文档、架构决策记录之间存在状态冲突：

1. 执行原则以本文件为最高优先级。
2. 具体流程以路线图为准。
3. 当前事实状态以最新架构决策记录、总底表更新报告和现场文件核验为准。
4. 发现冲突时先说明冲突，不得把旧状态写入新结果。

## 1. 项目定位

本项目用于建设珠三角三甲医院医生 BD 画像库。当前阶段不是生成对外营销文案，而是从医院官方公开渠道采集医生基础职业信息，结构化写入统一总底表，并在质量可控后生成 Obsidian 医生画像 Markdown，供后续分析、筛选、BD 跟进和个人 IP 包装前置使用。

第一阶段范围：

1. 地域：珠三角官方范围内城市，优先广州、深圳、东莞、珠海、中山等。
2. 医院：医院等级明确为三甲或三级甲等。
3. 医生：全院医生采集，重点关注慢性病、肿瘤、生殖疾病、免疫功能低下、术后恢复、疑难重症、难治疾病等方向。

## 2. 合规红线

允许来源：

1. 医院官网公开网页。
2. 医院官方医生介绍页。
3. 医院官方科室或专家介绍页。
4. 医院官方公开小程序或公众号内容，但脚本默认只处理公开网页，交互型入口需人工判断。

禁止行为：

1. 不使用第三方挂号平台、医生评价平台、非官方整理站。
2. 不采集患者评价、排名、评论、问诊内容。
3. 不采集私人手机号、私人微信、家庭住址、身份证号等隐私。
4. 不绕过登录、验证码、反爬或权限限制。
5. 不采集非公开接口或内部数据。
6. 不对医疗能力作无证据包装。
7. 不写保证治愈、治愈率承诺、疗效绝对化表达。

官网没有展示的信息直接留空。不得通过猜测、第三方搜索或营销改写补齐字段。

## 3. 固定数据资产

| 用途 | 路径 |
|---|---|
| 医生画像仓库 | `D:\workspace\信息收集整理\医生画像仓库` |
| 官网入口台账 | `D:\workspace\信息收集整理\医生画像仓库\99_资料来源\珠三角三甲医院官网入口台账.xlsx` |
| 统一医生总底表 XLSX | `D:\workspace\信息收集整理\医生画像仓库\99_资料来源\珠三角三甲医院_医生画像自动采集总底表.xlsx` |
| 统一医生总底表 CSV | `D:\workspace\信息收集整理\医生画像仓库\99_资料来源\珠三角三甲医院_医生画像自动采集总底表.csv` |
| 总底表更新报告 | `D:\workspace\信息收集整理\医生画像仓库\99_资料来源\珠三角三甲医院_医生画像自动采集总底表_更新报告.md` |
| 采集脚本 | `D:\workspace\信息收集整理\work\collect_official_doctors_batch.py` |
| Obsidian 画像生成脚本 | `D:\workspace\信息收集整理\work\generate_obsidian_profiles.py` |
| Obsidian 输出根目录 | `D:\workspace\信息收集整理\医生画像仓库\01_试点医院` |
| 医生画像模板 | `D:\workspace\信息收集整理\医生画像仓库\模板\医生画像模板.md` |
| GitHub 仓库 | `https://github.com/nancywrayg57-jpg/doctor-data-collection.git` |
| Codex 最新提示词入口 | `D:\workspace\信息收集整理\docs\agent_prompts\codex_next_prompt.md` |

禁止擅自删除：

1. 官网入口台账。
2. 统一总底表 XLSX/CSV。
3. 总底表更新报告。
4. 正式 Obsidian 医生画像。
5. 管理员人工修正后的文件。

清理临时文件前必须先列出将删除的精确文件路径，并说明受保护文件不会被删除。

## 4. 字段口径

统一总底表至少保留以下字段：

1. 医院。
2. 姓名。
3. 科室。
4. 职称。
5. 简介/擅长。
6. 重点关注范围。
7. 重点疾病标签。
8. 亮眼经历线索。
9. 来源链接。
10. 采集日期。
11. 异常提示。
12. 复核状态。

字段处理规则：

1. `医院`、`姓名`、`来源链接` 是核心追溯字段，缺失时不得直接生成正式画像。
2. 底表中职称只出现“医师”的，职称统一写“医师”。
3. 官网简介较短或没有简介时，相关字段留空。
4. 亮眼经历只摘录官方公开资料中可追溯的疑难重症、难治疾病、复杂病例、重要医疗经历等线索。
5. 重点标签只根据官方文本出现的疾病或方向标注，不强行推断。

## 5. 三方协作模型

当前固定模式：

1. 管理员：指定方向、设置确认/审计门禁、裁决异常口径。当前已授权：Claude 试采审计 `通过` 或 `有条件通过` 后，Codex 可直接正式追加，无需管理员二次确认。
2. Codex：作为本地 developer 执行采集、脚本维护、试采、追加、检查、清理、状态记录、Git 分支提交和交接；GitHub 账号为 `xtzhou247`。
3. Claude：作为 owner 独立审计样本或正式结果，检查字段错位、非医生页面混入、异常提示和合规风险；试采审计通过结论可作为正式追加门禁；GitHub owner 账号为 `nancywrayg57-jpg`。

职责边界：

1. Codex 不绕过试采门禁，不无复核批量写入全部医院。
2. Claude 不直接修改总底表；管理员已授权其试采审计通过结论作为 Codex 追加门禁。
3. 管理员不应继续逐个复制医生详情页链接；低效人工动作应优先转为台账、脚本或适配器能力。
4. Codex 与 Claude 对同一异常口径反复冲突时，由管理员裁决。

GitHub 协作口径：

1. 本任务采集结果、脚本和文档将上传到 `https://github.com/nancywrayg57-jpg/doctor-data-collection.git`。
2. Codex 只按 developer 身份工作：创建工作分支、提交实现、推送分支、发起 PR、按 Claude 审计意见返修；不得直接推送或合并 `main`。
3. Claude 只按 owner 身份工作：审计 Codex 结果、提出下一步指导、输出 Codex 可执行提示词、审批或合并 PR。
4. Claude 输出给 Codex 的下一步提示词必须写入或同步到 `D:\workspace\信息收集整理\docs\agent_prompts\codex_next_prompt.md`，确保 Codex 新会话可自动读取。
5. 当前本地目录若尚未初始化为 Git 仓库，先做只读审计并记录状态；初始化、绑定远端、推送和配置 GitHub 保护规则属于外部副作用，需管理员明确授权后执行。
6. Codex 执行任何推送、PR 或远端写入前必须核验 GitHub 登录身份是否为 `xtzhou247`；若当前身份为 `nancywrayg57-jpg`，只能执行 owner 审计或只读检查，不得冒用 owner 身份提交 Codex 实现。

## 6. 标准执行 SOP

### 6.1 执行前检查

1. 确认管理员已关闭正在编辑的总底表 Excel。
2. 确认入口台账、总底表、采集脚本存在。
3. 读取路线图和最新架构决策记录。
4. 列出入口台账中已确认 A 级医院。
5. 确认目标医院尚未正式追加到总底表。

### 6.2 选择医院

优先选择管理员指定医院；如管理员未指定，则按入口台账顺序选择下一家未追加的已确认 A 级医院。

当前已知状态以总底表更新报告为准：截至 2026-08-10 15:58，总底表包含 5 家医院、1993 位医生、37 条异常提示。下一家未追加 A 级医院在既有路线图中记录为 `南方医科大学口腔医院(海珠广场院区)`，执行前仍需现场核验。

### 6.3 小样本试采

每家医院正式追加前必须先试采 10 位医生，不写入总底表。

参考命令：

```powershell
cd D:\workspace\信息收集整理
python .\work\collect_official_doctors_batch.py --hospital "目标医院名称" --trial-only --max-doctors 10 --no-xlsx
```

试采后必须输出：

1. 试采 CSV 路径。
2. 试采报告路径。
3. 医院入口链接。
4. 适配器名称。
5. 样本摘要。
6. 异常提示。
7. 是否发现非医生页面混入。

等待管理员确认，或取得 Claude `通过` / `有条件通过` 审计结论后，才可进入正式追加。若已取得 Claude 试采审计通过结论，无需管理员二次确认。

### 6.4 正式追加

专用适配器医院：

```powershell
cd D:\workspace\信息收集整理
python .\work\collect_official_doctors_batch.py --hospital "目标医院名称"
```

通用模板医院必须显式授权追加：

```powershell
cd D:\workspace\信息收集整理
python .\work\collect_official_doctors_batch.py --hospital "目标医院名称" --allow-generic-append
```

正式追加后检查：

1. 新增写入数。
2. 重复跳过数。
3. 目标医院在总底表中是否出现。
4. 总底表 XLSX/CSV 和更新报告修改时间是否更新。
5. `异常提示` 数量和内容是否集中异常。

若单家医院医生数超过 700，使用更长超时或监控进程，不得重复启动并发写表。

正式追加并检查总底表后，必须先生成或补充本院 Obsidian 医生画像，并核验画像数量和 `_索引.md` 链接数；画像完成后才进入下一家医院。

### 6.5 清理临时文件

正式追加并检查完成后，才清理本院试采临时文件。可清理范围仅限：

1. `D:\workspace\信息收集整理\work\*_trial_payload.json`
2. `D:\workspace\信息收集整理\work\*_trial_doctors.csv`
3. `D:\workspace\信息收集整理\work\*_trial_doctors.xlsx`
4. `D:\workspace\信息收集整理\work\*_trial_report.md`
5. `D:\workspace\信息收集整理\work\*_official_doctors_preview.png`

不得使用不受控的递归删除命令。

## 7. Obsidian 画像规则

Obsidian 医生画像只允许基于统一总底表字段生成。

生成前必须检查：

1. 总底表记录数。
2. 目标医院记录数。
3. 已有 Markdown 数量。
4. 每家医院 `_索引.md` 是否存在。
5. 索引链接数与医生画像数是否一致。
6. 是否存在文件名覆盖、重名医生、异常姓名、非医生页面误识别。

当前状态提醒：既有文档显示 Obsidian 画像脚本已经升级并执行过补充生成，但现场文件数量与部分报告数字存在不一致。任何新会话进入 Obsidian 续跑或验收前，必须重新做数量核验，不得直接引用旧报告作为完成证据。

生成规则：

1. 不覆盖人工精修画像，除非管理员明确授权。
2. `异常提示` 不为空的记录应保留提示或进入异常清单。
3. 姓名明显不是医生姓名、来源页明显为科室页或介绍页的记录，不直接生成正式画像。
4. 画像内容不得生成疗效承诺、排名、患者评价或无来源亮点。

## 8. 脚本与适配器边界

当前采集脚本定位是官方渠道约束下的半自动批量采集工具，不是一键无人值守全自动采集器。

原则：

1. 通用官网模板先试采，质量合格再正式追加。
2. 通用模板表现稳定的官网不单独开发适配器。
3. 通用模板误识别严重的官网，停止正式追加，记录为待处理医院或新增专用适配器。
4. 不建议每家医院从零开发专用适配器。
5. 不建议管理员逐个复制医生详情页链接。

## 9. 诊断与熔断

处理异常必须执行：

1. 读报告。
2. 看样本。
3. 定位入口或适配器原因。
4. 提出最小修正方案。
5. 再执行试采验证。

出现以下情况立即停止，并输出 `[FATAL - HUMAN_INTERVENTION_REQUIRED]`：

1. 连续 2 次修复或采集验证失败。
2. 发现第三方平台或非官方来源混入。
3. 发现患者隐私、评价、登录后数据被采集。
4. 总底表写入疑似重复启动或文件被占用导致不一致。
5. Codex 与 Claude 对同一口径冲突超过 3 次。
6. 架构红线被违反且无法自行修复。

## 10. 文档沉淀

聊天记录不是长期事实源。每完成一个业务子模块，必须将核心状态和架构决策沉淀到：

```text
D:\workspace\信息收集整理\docs\architecture_decisions
```

沉淀内容应包括：

1. 目标。
2. 已完成动作。
3. 关键文件。
4. 当前数据状态。
5. 风险和下一步。
6. 后续 Agent 应读取的最小上下文。

## 11. Handoff_State 模板

跨 Agent 或跨会话交接必须输出以下块：

```text
<Handoff_State>
Target: 珠三角三甲医院官方医生画像采集
AgentConstitution: D:\workspace\信息收集整理\Agent.md
RouteDoc: D:\workspace\信息收集整理\docs\2026-08-10_医生画像采集执行路线图.md
RequirementDoc: D:\workspace\信息收集整理\docs\2026-08-10_医生画像采集任务需求确认.md
GitHubRepo: https://github.com/nancywrayg57-jpg/doctor-data-collection.git
CodexDeveloper: xtzhou247
ClaudeOwner: nancywrayg57-jpg
CodexPromptInbox: D:\workspace\信息收集整理\docs\agent_prompts\codex_next_prompt.md
Ledger: D:\workspace\信息收集整理\医生画像仓库\99_资料来源\珠三角三甲医院官网入口台账.xlsx
MasterTable: D:\workspace\信息收集整理\医生画像仓库\99_资料来源\珠三角三甲医院_医生画像自动采集总底表.xlsx
Completed:
- 写明本轮已完成采集、追加、审计、画像生成或文档更新事项
CurrentFacts:
- 写明当前总底表医院数、医生数、异常提示数和已核验时间
Next:
- 写明下一步医院、是否需要试采、是否需要 Claude 审计
Constraints:
- 仅官方公开渠道
- 禁止第三方平台、患者评价、隐私、登录/验证码绕过
- 先试采 10 位；管理员确认或 Claude 审计通过后正式追加
- 正式追加后先生成 Obsidian 画像，再进入下一家医院
- 官网没有的信息留空，不补造
Artifacts:
- 写明本轮生成的 CSV、报告、总底表、画像或审计材料路径
</Handoff_State>
```

无 `<Handoff_State>` 的跨会话交接视为不完整。
